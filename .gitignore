node_modules/
dist/
.nx/
coverage/
*.tsbuildinfo
*.tgz
# Same tarball copied into the docker build context so the image can install the
# `spacemolt` CLI from it (Docker COPY can only read files inside the build context)
!packages/domain-spacemolt/src/docker/*.tgz
.ds_store
__pycache__/
.env
.oauth-token
auth-token
.claude-credentials.json
.claude/
repos/

players/
config.json
test-output.log
